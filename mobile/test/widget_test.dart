import 'package:flutter_test/flutter_test.dart';
import 'package:wifi_motion_sensing/main.dart';

void main() {
  test('WifiMotionApp can be created', () {
    const app = WifiMotionApp();
    expect(app, isA<WifiMotionApp>());
  });
}
